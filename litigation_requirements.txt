# Requirements for Private AI Litigation Knowledge System

# Core Gradio (already in main requirements.txt)
gradio

# Environment Variables and Secrets Management
python-dotenv>=1.0.0

# Document Processing
PyPDF2>=3.0.0
python-docx>=1.0.0
pandas>=1.0.0
openpyxl>=3.0.0  # For Excel file support

# Vector Embeddings and Search
sentence-transformers>=2.2.0
faiss-cpu>=1.7.4  # Use faiss-gpu if you have CUDA
numpy>=1.0.0

# PyTorch (CPU-only for Windows compatibility)
# NOTE: On Windows, PyTorch may have DLL issues. If you encounter errors:
# 1. Run: .\fix_pytorch.ps1
# 2. Or manually install: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
# PyTorch will be installed automatically by sentence-transformers, but Windows users
# may need to reinstall with the CPU-only version for better compatibility.

# Optional: For better email processing
# extract-msg  # For .msg files (Windows Outlook)

# Optional: For advanced AI capabilities
# transformers>=4.20.0  # If you want to use local LLMs
# torch>=1.12.0  # If using transformers

# Optional: For better text extraction
# pdfplumber>=0.9.0  # Alternative PDF processor
# python-docx2txt>=0.8  # Alternative DOCX processor

# Optional: Google Drive Integration
# google-api-python-client>=2.0.0
# google-auth-httplib2>=0.1.0
# google-auth-oauthlib>=1.0.0

